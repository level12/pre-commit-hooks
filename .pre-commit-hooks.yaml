-   id: check-ruff-versions
    name: check ruff versions
    description: Ensure ruff version is the same in repo hook config and project dependencies
    entry: check-ruff-versions
    language: python
    files: '/requirements/dev\.txt$|^.pre-commit-config.ya?ml$|^prek\.toml$|/uv.lock$'
